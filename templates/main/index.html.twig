{% extends 'base.html.twig' %}

{% block title %}
    Hello!
{% endblock %}

{% block body %}
    <p>
        {{ 'info_hello_world'| trans }}
    </p>
    <div class="form-group row float-sm-left">
        <a href="{{ url('register_form') }}" class="btn btn-lg btn-primary">
            {{ 'action_open_register_panel'| trans }}
        </a>
    </div>
    <div class="form-group row float-sm-right">
        <a href="{{ url('app_login') }}" class="btn btn-lg btn-primary">
            {{ 'action_open_login_panel'| trans }}
        </a>
    </div>

{% endblock %}